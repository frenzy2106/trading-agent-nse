# secrets
.env
.kite_session.json

# python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
env/

# reports (committed selectively via samples/)
reports/
backtest/
cache/
screener/

# editor / tooling
.vscode/
.idea/
.claude/
*.swp

# stray data dumps
*.csv

# local html scratch (explainers, notes, scratch pages)
*.html

# RAG corpus + Chroma vectorstore (regenerate locally via ingest/build_concall_index.py)
data/
